<section id="workflow" class="container">
  <div class="section-head reveal">
    <span class="kicker" data-i18n="workflow.kicker">// workflow</span>
    <h2 data-i18n="workflow.title" data-fun-fx="title">How I build</h2>
    <p class="workflow__sub reveal" style="--d:.08s" data-i18n="workflow.sub">AI-augmented, but human-owned.</p>
  </div>

  <div class="workflow__grid">
    <div class="workflow__cards">
      <div class="glass workflow__card reveal" style="--d:.1s">
        <span class="workflow__icon" aria-hidden="true">
          <svg viewBox="0 0 24 24" width="20" height="20" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round">
            <rect x="3" y="11" width="18" height="11" rx="2" />
            <path d="M7 11V7a5 5 0 0 1 10 0v4" />
          </svg>
        </span>
        <h3 data-i18n="workflow.card1.title">Secure local AI</h3>
        <p data-i18n="workflow.card1.desc">Run LLMs locally (Ollama) so client and government code never leaves a secure environment.</p>
      </div>

      <div class="glass workflow__card reveal" style="--d:.2s">
        <span class="workflow__icon" aria-hidden="true">
          <svg viewBox="0 0 24 24" width="20" height="20" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round">
            <path d="M13 2L3 14h9l-1 8 10-12h-9l1-8z" />
          </svg>
        </span>
        <h3 data-i18n="workflow.card2.title">AI-accelerated delivery</h3>
        <p data-i18n="workflow.card2.desc">Cursor and coding agents draft scaffolding, migrations and tests. I cut delivery cycles without cutting quality.</p>
      </div>

      <div class="glass workflow__card reveal" style="--d:.3s">
        <span class="workflow__icon" aria-hidden="true">
          <svg viewBox="0 0 24 24" width="20" height="20" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round">
            <path d="M12 2l2.9 6.3 6.9.8-5.1 4.7 1.4 6.8L12 17.2 5.9 20.6l1.4-6.8L2.2 9.1l6.9-.8L12 2z" />
          </svg>
        </span>
        <h3 data-i18n="workflow.card3.title">Human-reviewed, always</h3>
        <p data-i18n="workflow.card3.desc">AI proposes. I own it: review, refactor and ship production-grade code.</p>
      </div>
    </div>

    <div class="glass arch reveal" style="--d:.2s">
      <h3 class="arch__title" data-i18n="workflow.arch.title">Decoupled by design</h3>
      <div class="arch__diagram" aria-label="Decoupled architecture diagram">
        <div class="arch__node">
          <span class="arch__node-title" data-i18n="workflow.arch.drupal">Drupal CMS</span>
          <span class="arch__node-sub" data-i18n="workflow.arch.drupalSub">content model · RBAC · workflows</span>
        </div>
        <div class="arch__link">
          <span class="arch__flow-line" aria-hidden="true"><span class="arch__flow"></span></span>
          <span class="arch__link-label" data-i18n="workflow.arch.api">JSON:API · OAuth2</span>
        </div>
        <div class="arch__node">
          <span class="arch__node-title" data-i18n="workflow.arch.react">React Frontend</span>
          <span class="arch__node-sub" data-i18n="workflow.arch.reactSub">dynamic UI · fast loads</span>
        </div>
      </div>
      <p class="arch__why" data-i18n="workflow.arch.why">Headless lets content teams publish independently while the frontend ships faster. 30% load-time gains and independent scaling.</p>
    </div>
  </div>
</section>

<style>
  .workflow__sub {
    margin-top: 0.8rem;
    color: var(--muted);
    font-family: var(--font-mono);
    font-size: 0.9rem;
  }

  .workflow__grid {
    display: grid;
    grid-template-columns: 1fr 1.1fr;
    gap: 1.2rem;
    align-items: stretch;
  }

  .workflow__cards {
    display: flex;
    flex-direction: column;
    gap: 1.2rem;
  }

  .workflow__card {
    padding: 1.4rem 1.6rem;
    transition: transform 0.3s var(--ease), border-color 0.3s;
  }

  .workflow__card:hover {
    transform: translateY(-3px);
    border-color: color-mix(in srgb, var(--accent-1) 45%, var(--glass-border));
  }

  .workflow__icon {
    display: grid;
    place-items: center;
    width: 2.4rem;
    height: 2.4rem;
    border-radius: 0.75rem;
    margin-bottom: 0.9rem;
    color: var(--accent-2);
    background: color-mix(in srgb, var(--accent-2) 10%, transparent);
    border: 1px solid color-mix(in srgb, var(--accent-2) 25%, transparent);
  }

  .workflow__card h3 {
    font-size: 1rem;
    font-weight: 700;
    margin-bottom: 0.4rem;
  }

  .workflow__card p {
    color: var(--muted);
    font-size: 0.92rem;
  }

  .arch {
    padding: 1.8rem;
    display: flex;
    flex-direction: column;
  }

  .arch__title {
    font-size: 1.05rem;
    font-weight: 700;
    margin-bottom: 1.4rem;
  }

  .arch__diagram {
    display: flex;
    align-items: center;
    gap: 1.2rem;
    flex: 1;
  }

  .arch__node {
    flex: 0 0 auto;
    display: flex;
    flex-direction: column;
    gap: 0.25rem;
    padding: 1.1rem 1.3rem;
    border-radius: var(--radius-sm);
    background: color-mix(in srgb, var(--accent-1) 8%, transparent);
    border: 1px solid var(--glass-border);
    min-width: 0;
  }

  .arch__node-title {
    font-weight: 700;
    font-size: 0.95rem;
    white-space: nowrap;
  }

  .arch__node-sub {
    font-size: 0.72rem;
    color: var(--faint);
    white-space: nowrap;
  }

  .arch__link {
    flex: 1;
    position: relative;
    display: flex;
    flex-direction: column;
    align-items: center;
    gap: 0.4rem;
    min-width: 90px;
  }

  .arch__flow-line {
    position: relative;
    width: 100%;
    height: 2px;
    background: linear-gradient(90deg, var(--accent-1), var(--accent-2));
    border-radius: 2px;
    opacity: 0.8;
  }

  .arch__flow {
    position: absolute;
    top: -5px;
    left: 0;
    width: 12px;
    height: 12px;
    border-radius: 50%;
    background: var(--accent-2);
    box-shadow: 0 0 12px var(--accent-2);
    animation: arch-flow 2.2s linear infinite;
  }

  @keyframes arch-flow {
    from { left: 0; opacity: 1; }
    to { left: calc(100% - 12px); opacity: 1; }
  }

  .arch__link-label {
    font-family: var(--font-mono);
    font-size: 0.75rem;
    color: var(--accent-2);
    text-align: center;
    white-space: nowrap;
  }

  .arch__why {
    margin-top: 1.4rem;
    padding-top: 1.1rem;
    border-top: 1px dashed var(--glass-border);
    color: var(--muted);
    font-size: 0.9rem;
  }

  @media (prefers-reduced-motion: reduce) {
    .arch__flow {
      animation: none;
      left: 50%;
    }
  }

  @media (max-width: 920px) {
    .workflow__grid {
      grid-template-columns: 1fr;
    }
  }

  @media (max-width: 600px) {
    .arch__diagram {
      flex-direction: column;
    }
    .arch__node {
      align-self: stretch;
      text-align: center;
    }
    .arch__link {
      width: 100%;
      min-width: 0;
      flex-direction: row;
      justify-content: center;
      gap: 0.7rem;
      margin-block: 0.2rem;
    }
    .arch__flow-line {
      width: 2px;
      height: 32px;
      background: linear-gradient(180deg, var(--accent-1), var(--accent-2));
    }
    .arch__flow {
      top: 0;
      left: -5px;
      animation: arch-flow-v 2.2s linear infinite;
    }
    .arch__link-label {
      white-space: normal;
      max-width: 75%;
    }
  }

  @keyframes arch-flow-v {
    from { top: 0; opacity: 1; }
    to { top: calc(100% - 12px); opacity: 1; }
  }
</style>